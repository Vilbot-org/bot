module.exports = async (client, interaction, snipe) => {
	const languageToSet = interaction.options.getString("language");

	/* const guildData = await snipe.findById(interaction.guildId);

	if (!guildData) {
		console.log("no existo");
	} */
};

# Vilbot

## Docker

### Build the image

```Shell
docker build -t vilbot .
```

### Run the image

Run the container in background

```Shell
docker run -d vilbot
```

Run in interaction mode

```Shell
docker run -it vilbot
```
